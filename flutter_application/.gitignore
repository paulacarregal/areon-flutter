# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Environment / secrets
.env
.env.*
!.env.example

# Server credentials
serviceAccount.json
firebase-adminsdk-*.json
google-credentials.json
*-service-account.json

# Private keys / certificates
*.pem
*.key
*.p12
*.pfx

# Local secret directories
secrets/
.secret/

# Firebase local cache/config
.firebase/

# Local macOS Firebase file. Android/Web/iOS Firebase config is part of the
# academic delivery so the app runs against the same project used in the MVP.
macos/Runner/GoogleService-Info.plist
